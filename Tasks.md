C /feature-dev:feature-dev Add support for selecting multiple items with CTRL/cmd or drag to select multiple objects. Add ability to group items with CTRL+G, double click to allow editing objects within a group. Add a new dockable pane for object hiearchy, default at the bottom of the pane. and also a possibly break out the group into a separate drawing file as reusable 'components'. On the same way, it should be possible to reuse another drawing (SVG) in a drawing with a mount button or drag and dropping an SVG into the drawing creating at the pointer position. 
C /feature-dev:feature-dev Add support for cut, copy and paste selected objects with standardized shortcuts and menu options provided by OS/wxWidgets.
/feature-dev:feature-dev Add support for snapping of elements, both proportionally and proximately (center vertical horizontal to parent group, or underlying object), snapping of vectors in bezier editing to other beizer points in the same object or any other objects in the drawing.
/feature-dev:feature-dev Add a new document type, .smil document which is like SVG but adds support for multiple slides that acts as 'scenes' consists of SVG elements that has the same editing engine as SVG, which has support for keyframe animation of all property types with choice of linear, beizer or cubic interpolation in a new SMIL based derivative of the .svg type and document/view editor, based on SVG xml which adds support for keyframe animation of all current and future properties of all now and future object types, with a dockable keyframe panel, default docked at the top like in the older versions of Macromedia (a.k.a Adobe) Flash studio, which lists all objects with expandable properties as keyframe timelines, with keyframe symbols attached to properties in the property pane. Add a new horizontal toolbar to the top, just after the one with standard options, with play backward and forward (we will implement that soon) with a rec button in middle, which will toggle if keyframe animation editing mode is enabled. This subtype adds new paremeters to this file type such duration (stored as seconds but displayed and edited as a wxWidgets Duration field). animation data is stored in SVG smil tags. In property editor allow editing any xml attribute of the selected object. When creating a new .spmx project, it should create an index.smil whose editing is the document viewed as the project document view instead of the current stats page. 
/feature-dev:feature-dev Create a new document type, .spmx, (Swim Animator Movie file), an XML based format which can house a set of .smil files in custom order in a storyboard shown as a tab in the bottom panel which now house a single tab with properties, that is a storyboard to become scenes in a editable movie storyboard, provided as a (at default at bottom) dockable panel at the bottom. Apart to playing the scens, it should support a timeline which allows adding music and voice imported to the project assets.
/feature-dev:feature-dev Add support for reusing any other .smil file in a project as ref with editable location in the editor.
/feature-dev:feature-dev Add support for text file editing using a third party wxWidgets compatible Syntax highlighting editor.
Current and future specific Swim features is stored in the namespace xmlns:swim. All links src is relative to the project directory.
/feature-dev:feature-dev Add support for exporting the .spm as a .ogv through ffmpeg.
/feature-dev:feature-dev Add support for interactivity to the .svg and .smil, based on SMIL interactivity in JS, with flash like code editing panels, utilizing wxWidgets code editor.
