# DONE
1	F /feature-dev:feature-dev Add support for cut, copy and paste selected objects with standardized shortcuts and menu options provided by OS/wxWidgets.
2	F /feature-dev:feature-dev Add support for snapping of elements, both proportionally and proximately (center vertical horizontal to parent group, or underlying object), snapping of vectors in bezier editing to other beizer points in the same object or any other objects in the drawing.
3	F /feature-dev:feature-dev Add support for selecting multiple items with CTRL/cmd or drag to select multiple objects. Add ability to group items with CTRL+G, double click to allow editing objects within a group. Add a new dockable pane for object hiearchy, default at the bottom of the pane. and also a possibly break out the group into a separate drawing file as reusable 'components'. On the same way, it should be possible to reuse another drawing (SVG) in a drawing with a mount button or drag and dropping an SVG into the drawing creating at the pointer position.
4	F /feature-dev:feature-dev Add a new document type, .smil document which is like SVG but adds support for multiple slides that acts as 'scenes' with keyframe animation, dockable keyframe panel, animation toolbar, XML attribute editor, and scene panel.
